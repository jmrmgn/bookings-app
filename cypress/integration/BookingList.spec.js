/* eslint-disable jest/valid-expect */
/// <reference types="cypress" />

describe('List of Bookings', () => {
  it('Should be able to show the list of Bookings', () => {
    //
  });

  it('Should be able to filter and sort the table', () => {
    //
  });
});
