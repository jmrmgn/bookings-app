/* eslint-disable jest/valid-expect */
/// <reference types="cypress" />

describe('Delete of Booking', () => {
  it('Should be able to delete a booking', () => {
    //
  });

  it('Must not delete a booking if cancel is clicked', () => {
    //
  });
});
